::error file=services/api/main.go,line=8::cross-unit: api depends on billing (denied by boundary "services") — units `api` and `billing` are peers in the `services` boundary (its members include `api` and `billing`), which is mutually exclusive, so neither may depend on the other. Fix: extract the shared code into a unit outside `services`, or remove one member from the boundary.
::error file=shared/index.ts,line=3::cross-unit: shared depends on web (shared: deny [*]) — unit `shared` explicitly denies depending on `web` (its deny list in depdog.work.yaml names `web`). Fix: remove `web` from `shared`'s deny list if the dependency is intended, or drop the reference to `web`.
::error file=web/src/app.ts,line=4::cross-unit: web depends on shared/internal (shared: internal [internal/**]) — `web` reaches into `shared`'s internal paths: `shared/internal` matches `internal/**`, which `shared` declares internal. Fix: depend on `shared` through its exported surface (`src/**`), or widen `shared`'s surface in depdog.work.yaml.
depdog check — monorepo · 0 violations · 2 packages across 2 checked units · 3 cross-unit violations
