::error file=web/src/api/server.ts,line=7::api imports ../domain/order (api: allow [service, std, external]) — `web/src/api` (component `api`) may import only `service`, `std`, `external`; `../domain/order` (component `domain`) is not among them. Fix: add `domain` to `api`'s allow list, or depend only on what `api` already allows.
depdog check — monorepo · 1 violation · 10 packages across 3 checked units
