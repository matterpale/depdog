{
  // depdog fixture: exercises baseUrl + a single paths alias.
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@app/*": ["src/*"]
    },
    "strict": true,
    "module": "ESNext"
  }
}
