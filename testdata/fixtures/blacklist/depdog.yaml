# Blacklist style: everything is allowed except what is explicitly denied.
version: 2

components:
  domain:  { path: "internal/domain/**",  deny: [handler, service, external] }
  handler: { path: "internal/handler/**", deny: [service] }
  service: { path: "internal/service/**" }

default: allow
