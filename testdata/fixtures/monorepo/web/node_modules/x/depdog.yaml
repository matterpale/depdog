# DECOY: this lives under node_modules and MUST be invisible to discovery.
# If the walk ever reports a unit here, the skip-list pruning has regressed.
version: 2

components:
  x: { path: "**", allow: ["*"] }

default: allow
