{
  // depdog monorepo fixture: the TypeScript unit. baseUrl + one paths alias.
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@web/*": ["src/*"]
    },
    "strict": true,
    "module": "ESNext"
  }
}
